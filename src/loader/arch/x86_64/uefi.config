ARCH := x86_64
# TARGET := $(OUT_DIR)/$(LOADER_NAME)
# SRCS := $(LOADER_C_SRC_FILES) $(LOADER_CPP_SRC_FILES)
SRCS := $(shell find . -name "*.c")
CFLAGS = -pedantic -Wall -Wextra --std=c11 -O2
LDFLAGS =
LIBS = -lm
# OUTDIR = $(BUILD_DIR)

include uefi/Makefile
